clc             %Limpio el command window
close all       %Cierro todas las ventanas
clear all       %Limpio todas las variables del work space
 
%GENERACIÓN DE LOS DATOS
 
n=200;
input=linspace(0,2*pi,n);
target=cos(input); %que aprendar hacer consenos
plot (input,target);
 
save('Datos.mat','input','target'); %guardo los datos